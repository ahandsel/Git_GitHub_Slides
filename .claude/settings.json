{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath // empty' | { read -r f; case \"$f\" in *.md|*.json|*.jsonc|*.json5|*.yml|*.yaml|*.sh|*.toml|*.xml|*.svg|*.js) pnpm lint ;; esac; } 2>/dev/null || true",
            "statusMessage": "Running pnpm lint…"
          }
        ]
      }
    ]
  }
}
